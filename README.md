# stekoverflu
implementing SO in rails

### setup
change the mysql username and password, accordingly in `config/database.yml`
run `rake db:setup` or `rake db:create && rake db:migrate` or whatever you do that you do
